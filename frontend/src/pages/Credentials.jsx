function Credentials() {
    return (
        <p>Credentials</p>

        // if (logging in)
        // show login container
        // else if (signing up)
        // show sign up container
    )
}

export default Credentials;